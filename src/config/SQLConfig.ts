interface SQLConfigInterface {
    host: string;
    user: string;
    password: string;
    database: string;
}

const SQLConfig = {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'imdbtop250movies'
}

export default SQLConfig;
